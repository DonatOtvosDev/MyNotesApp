class NoteData {
  int id;
  String title;
  String content;
  DateTime lastModified;
  NoteData({
    required this.id,
    required this.title,
    required this.content,
    required this.lastModified,
  });
}